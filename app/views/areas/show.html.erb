<div class="fondop">
	<p id="notice"><%= notice %></p>
	<div class="pisos">
		<p>
	  		<%= @area.nombre %>
		</p>
		<p>
			<div>
	  			<%= image_tag (@area.image.url), height: '600', width: '600' %>
	  		</div>
		</p>
		<%= link_to 'ver elementos', cuentadante_elementos_path(area.cuentadante_id) %>
		<%= link_to 'Editar', edit_floor_area_path(@floor,@area) %> |
		<%= link_to 'Atras', floor_areas_path %>
	</div>
</div>