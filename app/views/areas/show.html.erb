<div class="showcentro">
	<div class="fondop">
		<p id="notice"><%= notice %></p>
		<div class="pisos">
			<p>
				<strong>Nombre Area:</strong>
		  		<%= @area.nombre %>
			</p>
			<%#<p>
				<div>
		  			<%= image_tag (@area.image.url), height: '300', width: '300' %>
		  		</div>
			<%#</p>%>
			<%= link_to 'Ver elementos ⇨ '+ @area.elementos.count.to_s + '', area_elementos_path(@area), class: "btn btn-default btn-sm" %>
			<hr>
			<div class="editar_area">
			<%= button_to 'Editar', edit_floor_area_path(@floor,@area), method: "get", class: 'btn btn-warning'%>
			</div>
			<div class="eliminar_area">
			<%= link_to 'Eliminar', floor_area_path(@floor,@area), method: :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => "Esta Seguro?")) }, method: "get", class: 'btn btn-danger' %>
			</div>
			<%= button_to 'Atras', floor_areas_path, method: "get", class: 'btn btn-success' %>
		</div>
	</div>
</div>