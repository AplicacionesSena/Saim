<%= form_for(@tipo_elem) do |f| %>
  <% if @tipo_elem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_elem.errors.count, "error") %> prohibited this tipo_elem from being saved:</h2>

      <ul>
      <% @tipo_elem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :Descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>
