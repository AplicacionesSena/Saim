<div class="showcentro">
<p id="notice"><%= notice %></p>

<p>
  <strong>Nombre Elemento:</strong>
  <%= @transfer.elemento.nombre %>
</p>

<p>
	<strong>Cuentadante Origen:</strong>
	<%= @transfer.cuentaorigen %>
</p>

<p>
  <strong>Cuentadante Destino:</strong>
  <%= @transfer.cuentadante.nombre %>
</p>

<p>
  <strong>Fecha Traspaso:</strong>
  <%= @transfer.fechaTraspaso %>
</p>
</div>
<div class="contdiv3">
	<div class="mo0a1-2">
	<%= button_to 'Editar', edit_elemento_transfer_path(@elemento,@transfer), method: "get", class: 'btn btn-warning' %>
	</div>
	<div class="mo0a2">
	<%= button_to 'Atras', elemento_transfers_path, method: "get", class: 'btn btn-success' %>
	</div>
</div>
