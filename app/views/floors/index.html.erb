<div class="divplan">
<h1 class="centro">Listado De Pisos</h1>
<div class="centro">
<%#<%= form_tag floors_path, :method => 'get' do %>
<%#  <% content_tag :label do %>
<%#    <div class="form-group">
      Buscar Piso:
      <%= text_field_tag :search, params[:search], class: 'form-control tams' %>
<%#      <%= image_submit_tag("busca.png", height: '16', width: '18', class: 'img11') %>
<%#    </div>
  <% end %>
<%#<% end %>
</div>
    <% @floors.each do |floor| %>
       <h1 class="centro"><%= floor.nombre %></h1><%#</td>%>
       <%= image_tag(floor.image.url)%><%#</td>%>
        <%#<td><%= link_to 'Ver', floor %><%#</td>%>
        <%#<td><%= link_to 'Editar', edit_floor_path(floor) %><%#</td>%>
        <%#<td><%= link_to 'Eliminar', floor, method: :delete, data: { confirm: 'Estas Seguro?' } %><%#</td>%><%= button_to 'Ver Areas ⇨ '+ floor.areas.count.to_s + '', floor_areas_path(floor), class: "btn btn-default movercentro" %>
       <div class="editar_indexareas">
        <%= button_to "Editar", edit_floor_path(floor), method: "get",class: 'btn btn-warning' %>
       </div>
       <hr>
    <% end %>
    <%= button_to 'Nuevo Piso', new_floor_path, method: "get", class: 'btn btn-primary movercentro' %>
<hr>
</div>

</div>